# Starter Library Compiled Upfront, No Runtime Web Scraping

The app ships with, and allows import of, a library compiled during development; it never searches or fetches content at runtime. Source credits are kept for provenance, but there is no rights-status workflow because the home already holds licences for the content it uses. Runtime web search was rejected because of network unreliability and the distraction it would add during a therapy group.
