# Content Delivered as Manual Import Packages

New content arrives as single-file import packages that the home installs when ready. There is no silent auto-update. In-app add-item remains available for home-specific or last-minute content, so therapists are never blocked while waiting for the next pack.
